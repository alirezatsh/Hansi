Django Project Guide - SQLite with Docker

Database:
You selected SQLite.
A Docker container has been created and is running your Django project.

The application is accessible at:
  http://localhost:8000

No need to run the server manually.
Create superuser inside the container if needed:
  docker exec -it <project_name> python manage.py createsuperuser
